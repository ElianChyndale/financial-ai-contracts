# Schema Changelog

## 1.0.0 ? repository v0.1.0

Initial public contract set:

- `EvidenceUnit`
- `RiskAttestation`
- `ValuationScenario`
- `BenchmarkCase`
- `ExperimentRecord`

The release defines the FAC Canonical JSON Profile, SHA-256 record hashes, explicit
UTC and decimal rules, a bounded Solidity risk-attestation projection, and two
example legacy migrations.

